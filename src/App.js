import { Button, Icon } from '@mui/material';
import './App.css';

function App() {
  return (
    <div>
      <Button variant="contained" color="primary">
        Hello World
      </Button>
      <Icon>add_circle</Icon>
    </div>
  );
}

export default App;
